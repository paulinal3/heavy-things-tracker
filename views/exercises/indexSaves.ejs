<h1>All Your Saved Exercises </h1>

<ul>
    <% exerciseSaves.forEach(exercise => { %>
        <li style="list-style-type:none">
            <% let name = exercise.name %>
            <% name.replace('', '%20') %>
            <a href="/exercises/saves/<%= exercise.id %>"><%= exercise.name %></a>
        </li>
    <% }) %> 
</ul>